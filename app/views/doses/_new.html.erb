
<h1>Add a dose</h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.association :ingredient, input_html: { class: 'select2' }, wrapper_html: { class: 'w-100' } %>
  <%= f.input :description, label: "Quantity" %>
  <%= f.submit "Add dose", class: "btn btn-primary" %>
<% end %>
